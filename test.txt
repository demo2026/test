/**
     * Parses a string of key-value pairs separated by ',' and '=' into a map.
     * <p>
     * Example input: "forgerock=123;digitial=456"
     * Resulting map: {ab=123, cd=456}
     *
     * @param input the input string containing key-value pairs
     * @return a map containing the parsed key-value pairs; empty map if input is null or empty
     */
